<%@page_location = "projects"%>
<div class="projectPageWrap">

   <%= link_to "stickido a new project", new_project_path, remote: true, class: "do-new-project" %>


    <h1>projects</h1>

    <div id="myModal"></div>

    <div id="my-projects">
        <% @projects.each do |project| %>
            <div class="container">
                <div id="project-<%= project.id %>">
                    <a href="/projects/<%= project.id %>/tasks" data-turbolinks="false">
                        <button class="btn btn-primary"><%= project.name %></button>
                    </a> 
                    <%= link_to 'Edit', edit_project_path(project),class: "edit-button", remote: true %> 
                        <%= link_to 'Delete', project_path(project),class: "delete-button", method: :delete, data: { confirm: "Are you sure you want to delete '#{project.name}'?" } %>
                    <div>
                <button class="go-to-stickies"> <a href="/projects/<%= project.id %>/tasks" data-turbolinks="false">
                        Do
                    </a> </button>
            <a href="/projects/<%= project.id %>/stickis" data-turbolinks="false">
                Sticki
            </a>
                </div>

		            
	                
	                
                </div>
            </div>

        <% end %>
    </div>
 
</div>


